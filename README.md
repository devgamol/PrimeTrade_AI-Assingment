# Trading Bot

A minimal Binance Futures testnet CLI bot project scaffold.

## Setup (Placeholder)
1. Create and activate a virtual environment.
2. Install dependencies using `pip install -r requirements.txt`.
3. Add your Binance testnet API credentials.

## Usage (Placeholder)
Run the CLI entrypoint:

```bash
python -m bot.cli
```